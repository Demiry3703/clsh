{
  "name": "@clsh/landing",
  "version": "0.0.1",
  "private": true,
  "description": "clsh.dev landing page — interactive demo",
  "scripts": {
    "dev": "npx serve .",
    "build": "echo \"Static site — no build needed\""
  }
}
